# Golf Pool Tracker — backend env vars
# Copy to .env and fill in values

# Optional: DataGolf API key (for future direct API access instead of scraping)
# DATAGOLF_API_KEY=

# Database path (defaults to ./golf.db)
# DATABASE_URL=sqlite:///./golf.db
